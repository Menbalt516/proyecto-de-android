<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Título de la Meta -->
    <TextView
        android:id="@+id/txtTituloMeta"
        android:text="Título de la Meta"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <!-- Descripción -->
    <TextView
        android:id="@+id/txtDescripcionMeta"
        android:text="Esta es la descripción de la meta..."
        android:textSize="16sp"
        android:paddingTop="8dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <!-- Lista de SubMetas -->
    <TextView
        android:text="Sub-Metas:"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingTop="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/txtProgreso"
        android:text="Progreso: 0%"
        android:textSize="16sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvSubMetas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <!-- Botón para asociar -->
    <Button
        android:id="@+id/btnAsociar"
        android:text="Asociar Evento"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:layout_marginTop="12dp"/>

    <Button
        android:id="@+id/btnGuardarSubMeta"
        android:text="Guardar"
        android:layout_marginTop="20dp"
        android:padding="12dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
</LinearLayout>
